[build]
  # 전역 설치(-g) 대신 로컬에 pnpm을 설치하여 충돌을 피합니다.
  command = "npm install pnpm@9.15.4 && ./node_modules/.bin/pnpm install --no-frozen-lockfile && ./node_modules/.bin/pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "development"
